apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - modelserver.yaml
  - serviceAccount.yaml

# Uncomment to set a different namespace
# namespace: vllm

# Uncomment to add a name prefix for uniqueness
# namePrefix: tms-
